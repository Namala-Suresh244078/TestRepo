We need to upgrade the Node.js runtime/buildpack for application:
ni-product-aggregation-service

Target Upgrade:
- Upgrade application runtime to Node.js 26 (latest approved enterprise target)
- Ensure full application compatibility with Node 26
- Ensure buildpack/runtime compatibility
- Ensure deployment pipelines continue working

Story Context:
- Existing buildpack version: nodejs_previous 1.8.39
- Previous Node upgrade attempt failed due to dependency compatibility issues, especially:
  - class-validator
- Previous upgrade was reverted because validation-related breaking changes impacted API behavior.
- This upgrade must be production-safe and minimize business logic changes.

Primary Goal:
Upgrade runtime/buildpack to Node 26 while preserving all existing application functionality, validation behavior, APIs, and deployment stability.

Critical Requirements:
- Do NOT blindly upgrade all dependencies.
- Only upgrade dependencies required for Node 26 compatibility.
- Keep business logic unchanged.
- Keep API contracts unchanged.
- Ensure validation behavior remains backward compatible.

Tasks:

1. Analyze Existing Runtime Configuration
Identify ALL places where Node version/runtime/buildpack is configured, including:
- .nvmrc
- package.json
- package-lock.json
- CI/CD pipeline configs
- Dockerfile
- buildpack manifests
- Cloud Foundry manifests
- Jenkins/GitHub Actions configs
- deployment YAML files
- shell scripts
- npmrc files

2. Upgrade Runtime to Node 26
Update all runtime configurations to Node 26:
- .nvmrc
- package.json engines
- buildpack/runtime configs
- deployment manifests
- CI/CD configs

Expected:
- Local build uses Node 26
- Pipeline uses Node 26
- Deployment runtime uses Node 26

3. Dependency Compatibility Analysis
Perform dependency compatibility analysis for Node 26.

Identify:
- unsupported packages
- deprecated packages
- native modules
- old webpack tooling
- deprecated crypto usage
- deprecated Node APIs
- CommonJS/ESM incompatibilities
- node-gyp/native build issues

Special focus areas:
- class-validator
- class-transformer
- NestJS dependencies
- RxJS
- webpack
- ts-node
- jest
- eslint
- babel
- node-sass
- old OpenSSL usage

4. Upgrade Only Required Dependencies
Upgrade ONLY packages required for:
- Node 26 compatibility
- build stability
- runtime stability
- security compatibility

Avoid unnecessary major upgrades unless required.

5. Handle class-validator Carefully
This is the highest risk area.

If class-validator upgrade is required:
- identify all breaking changes
- compare old vs new validation behavior
- preserve existing API validation responses
- preserve DTO validation semantics
- preserve optional/required field behavior
- preserve transformation behavior
- preserve error response structure

Review:
- DTO decorators
- ValidationPipe configuration
- transform settings
- whitelist behavior
- forbidNonWhitelisted behavior

6. Resolve Node 26 Runtime Issues
Fix compatibility issues related to:
- OpenSSL changes
- deprecated crypto APIs
- Buffer deprecations
- fs API changes
- ESM/CommonJS conflicts
- webpack incompatibilities
- legacy loaders/plugins
- native module compilation

7. Package Lock Consistency
Ensure:
- package-lock.json regenerated correctly
- reproducible installs
- no invalid dependency trees
- no duplicate dependency conflicts

8. Build Validation
Run and fix:
- npm install
- npm run build
- npm run lint
- npm test
- npm start

Ensure zero runtime failures.

9. API Validation
Validate:
- all GET APIs
- all POST APIs
- DTO validation behavior
- authentication flows
- downstream integrations
- serialization/deserialization behavior
- error handling
- response formats

10. CI/CD Validation
Ensure:
- buildpack recognizes Node 26
- deployment pipeline succeeds
- no buildpack staging failures
- no runtime container issues
- no startup crashes after deployment

11. Production Safety Validation
Verify:
- application startup time
- memory usage
- logging
- HTTPS/TLS behavior
- environment variable loading
- health endpoints
- graceful shutdown behavior

12. Documentation
Provide summary of:
- runtime changes
- dependency upgrades
- compatibility fixes
- validation fixes
- pipeline changes
- known risks
- testing evidence

Expected Deliverables:
- Fully Node 26 compatible application
- Successful local build
- Successful tests
- Successful application startup
- Successful deployment pipeline
- Minimal production-safe code changes
- Clean package-lock.json
- Validation/testing evidence
- Summary of all compatibility fixes

Recommended Safe Upgrade Procedure:

Step 1:
Switch local environment to Node 26

Example:
- nvm install 26
- nvm use 26

Step 2:
Update runtime config files:
- .nvmrc
- package.json engines
- buildpack configs

Step 3:
Clean install:
- remove node_modules
- remove package-lock.json ONLY if necessary
- run npm install

Step 4:
Identify compatibility failures before upgrading dependencies.

Step 5:
Upgrade ONLY required packages.

Step 6:
Run build/test/start validation.

Step 7:
Validate APIs and DTO validations carefully.

Step 8:
Validate deployment pipeline/buildpack behavior.

Important Constraints:
- Do not refactor business logic.
- Do not redesign validation architecture.
- Do not migrate frameworks unless mandatory.
- Keep changes minimal and production-safe.
- Prioritize backward compatibility.
- Avoid unnecessary package churn.
- Preserve API response contracts.

Final Output Required:
1. List of runtime changes
2. List of dependency upgrades
3. Node 26 compatibility fixes
4. Validation fixes performed
5. Build/test evidence
6. Remaining risks/issues
7. Deployment validation summary